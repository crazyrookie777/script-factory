# 시작하기 — 이 한 장만 보면 됩니다

> 주식을 몰라도 됩니다. **종목 이름만 입력하면** 나머지는 AI가 다 합니다.
> 영상 1편에 **15분**이면 됩니다.

---

## 처음 한 번만 (10분)

### 1. Python 설치 확인

```bash
python --version
```

`Python 3.7` 이상이면 됩니다. 없으면 [python.org](https://www.python.org/downloads/)에서 설치하세요.
설치할 때 **"Add Python to PATH"** 체크 필수입니다.

### 2. VPN을 한국으로 연결

데이터를 한국 서버에서 가져옵니다. **VPN 서버는 한국이 가장 빠릅니다.**

### 3. 연결 점검

```bash
python _tools/collect.py --doctor
```

`전부 정상입니다`가 나오면 준비 끝입니다.
안 나오면 VPN을 다시 연결하고 한 번 더 실행하세요.

### 4. 기준대본 읽어보기

**`기준대본.txt`를 한 번 읽어 보세요.** 대표가 승인한 정답 샘플입니다.
앞으로 만들 대본이 이런 모양이면 됩니다. 5분이면 읽습니다.

---

## 매일 하는 일 — 3단계

### ① 데이터 뽑기 (1분)

```bash
python _tools/collect.py 삼성전자 > brief.md
```

한글 이름이 안 되면 6자리 코드로: `python _tools/collect.py 005930 > brief.md`

> ⚠️ `> brief.md` 뒤에 아무것도 붙이지 마세요. `2>&1` 같은 거 붙이면 파일이 깨집니다.


> ## ⚠️ Codex는 반드시 이 폴더에서 여세요
>
> ```
> ❌  아무 데서나 codex 실행  →  규칙이 하나도 안 걸립니다
> ⭕  이 폴더로 이동한 뒤 codex 실행
> ```
>
> Codex는 **현재 폴더의 `AGENTS.md`를 자동으로 읽습니다.**
> 폴더가 틀리면 그냥 평범한 AI가 돼서 대본이 엉뚱하게 나옵니다.
>
> 터미널에서 폴더로 이동하는 법 — 폴더 주소창에 `cmd` 입력 후 엔터

### ② 대본 만들기 (10분)

Codex를 **이 폴더에서** 열고, 아래를 **그대로 복사해서** 입력하세요.

```
AGENTS.md, 기준대본.txt, 09_변형뱅크.md, 10_고정문구_변형.md 를 먼저 읽어줘.

그다음 brief.md 로 대본을 만들어줘. 규칙은 이렇게:
- 고정 문구(후크/인사/CTA/마무리 등)는 10_고정문구_변형.md 에서 카테고리마다 하나씩 랜덤으로 골라 쓴다.
- 본론 문장은 09_변형뱅크.md 에 있는 원본 문장 중에서 골라 쓴다. 새로 지어내지 않는다.
- 뱅크 문장을 쓸 때 어미는 구어체로 바꾼다 (~습니다 → ~어요/~거든요). 목표 구어 60~70%.
- 지난 대본과 겹치지 않게 뱅크에서 다른 번호를 고른다.
- brief.md 에 없는 숫자는 절대 쓰지 않는다.
- 반문형(이해되시죠? / 감 오시죠? 등)을 7개 이상 넣는다.

대본은 대본.txt 로 저장하고,
python _tools/validate.py 대본.txt brief.md 를 직접 실행해서
✅ 합격 나올 때까지 고쳐줘.

마지막에 제목 10개, 썸네일 문구, 설명란, 태그도 같이 줘.
```

**이 프롬프트를 통째로 쓰세요.** 줄여서 쓰면 대본이 매번 비슷해지거나 이상한 말투가 나옵니다.

### ③ 검증 확인 (4분)

AI가 알아서 돌리지만, **직접 한 번 더 돌려서 눈으로 확인하세요.**

```bash
python _tools/validate.py 대본.txt brief.md
```

**✅ 합격이 나와야만 다음으로 넘어갑니다.**

```
   ✅  합격 — 업로드해도 됩니다
   분량 3,935자 / 9분 18초

   ★ 일레븐랩스에는 반드시 이 파일을 넣으세요
     → TTS용_대본.txt
```

❌ 불합격이면 화면에 나온 내용을 **그대로 복사해서** Codex에 붙여넣으세요.

```
검증에서 이렇게 나왔어. 전부 고치고 다시 검증해줘.
(붙여넣기)
```

보통 1~3번 만에 통과합니다.

---

## ④ TTS — 파일을 잘못 넣으면 다시 찍어야 합니다

합격하면 **`TTS용_대본.txt`** 가 자동으로 생깁니다.

> # 일레븐랩스에는 `TTS용_대본.txt` 만 넣으세요.

| ❌ 이러면 안 됩니다 | 무슨 일이 벌어지나 |
|---|---|
| Codex 화면에서 대본 복사 | `[00:00]` `**굵게**` 를 **소리 내어 읽습니다** |
| `대본.txt`(원본)를 그대로 사용 | 숫자 콤마를 쉼표로 끊어 읽습니다 |

`TTS용_대본.txt`는 검증기가 타임코드·별표·영문·콤마를 전부 정리해서 만든 파일입니다.

음성 받으면 영웅문 차트 띄우고 녹화하시면 됩니다.

---

## ⑤ 업로드

제목·설명란·태그를 유튜브에 붙여넣으면 끝입니다.

---

# ⚠️ 절대 하면 안 되는 것 5가지

1. **검증을 건너뛰지 마세요.** `합격.txt`가 없으면 그 대본은 검증을 안 거친 겁니다.
2. **숫자를 직접 고치지 마세요.** `brief.md`에 있는 것만 씁니다.
3. **"얼마까지 간다"고 쓰지 마세요.** 목표가·수익률 제시는 금지입니다.
4. **"사세요/파세요"라고 하지 마세요.** "확인해야 할 조건"만 말합니다.
5. **`AGENTS.md`·`기준대본.txt`·`10_고정문구_변형.md`·`_raw` 폴더를 지우거나 옮기지 마세요.** 없으면 검증기가 망가집니다.

---

# 왜 이렇게까지 하나요

대본은 **사람이 안 읽고 바로 TTS로 들어갑니다.**

```
AI가 쓴 대본 → 일레븐랩스 → 녹화 → 업로드
                ↑ 사람이 읽는 단계가 없습니다
```

AI가 **없는 숫자를 지어내는 일이 있습니다.** 주가나 수급 숫자를 그럴듯하게 만들어냅니다.
주식을 모르면 절대 못 알아봅니다. 그대로 올리면 시청자가 확인하고 바로 지적합니다.

검증기는 대본의 **모든 숫자를 `brief.md`와 대조**합니다. 없는 숫자는 무조건 걸립니다.
**이게 이 도구의 가장 중요한 기능입니다.**

---

## 문제가 생기면

| 증상 | 해결 |
|---|---|
| `접속 실패` | VPN 켜고 `python _tools/collect.py --doctor` |
| `종목코드를 못 찾았습니다` | 6자리 숫자 코드로 입력 |
| 한글이 깨짐 | `chcp 65001` 먼저 실행 |
| `python: command not found` | Python 재설치 (Add to PATH 체크) |

더 자세한 내용은 **`상세매뉴얼.md`** 를 보세요.
