# 직원용 운영 매뉴얼

> **주식을 몰라도 됩니다.** 종목 이름만 입력하면 나머지는 전부 자동입니다.
> 하루에 영상 1편 만드는 데 **15분**이면 됩니다.

---

## A. 처음 한 번만 — 설치 (10분)

### 1) Python 설치 확인

터미널(명령 프롬프트)에 입력:

```bash
python --version
```

`Python 3.7` 이상이 나오면 됩니다. 안 나오면 [python.org](https://www.python.org/downloads/)에서 설치하세요.
설치할 때 **"Add Python to PATH"** 체크를 꼭 하세요.

### 2) 폴더 받기

회사에서 준 `안교수카피` 폴더를 통째로 바탕화면에 두세요. 안에 이런 게 있어야 합니다.

```
안교수카피/
├── AGENTS.md          ← Codex가 자동으로 읽습니다. 지우지 마세요
├── CLAUDE.md          ← Claude Code용. 지우지 마세요
├── _tools/
│   ├── collect.py     ← 데이터 수집기
│   └── gueoche-guard/ ← 말투 검사기
└── (문서들)
```

### 3) 연결 점검 ★VPN 켠 상태에서 하세요

```bash
python _tools/collect.py --doctor
```

**결과별 대응**

| 나온 결과 | 해야 할 일 |
|---|---|
| `전부 정상입니다` | 바로 B단계로 |
| `일부만 됩니다` | VPN 서버를 **한국 또는 일본**으로 바꾸고 다시 |
| `전부 실패했습니다` | VPN이 꺼져 있거나 끊긴 상태입니다. 다시 연결 |

> **VPN 관련해서 알아둘 것**
> - 데이터를 한국 서버(네이버 금융)에서 가져옵니다. **VPN 서버는 한국이 가장 빠르고, 일본도 괜찮습니다.**
> - 미국·유럽 서버로 연결하면 되긴 하지만 느립니다.
> - VPN이 중간에 끊기면 `접속 실패`가 뜹니다. 그때는 VPN 다시 연결하고 명령만 재실행하면 됩니다.
> - 수집은 보통 **1초 안에** 끝납니다. VPN을 거치면 5~15초 정도 걸립니다. 30초가 넘으면 VPN 상태를 의심하세요.
> - 도구가 3단계로 우회를 시도합니다(일반 접속 → 야후 → 우회 엔진). 그래도 안 되면 VPN 문제입니다.

**매일 아침 한 번씩 `--doctor`를 돌리는 걸 권합니다.** VPN이 조용히 끊겨 있는 걸 미리 잡을 수 있습니다.

### 접속이 막힐 때 — 우회 엔진 (거의 필요 없습니다)

`collect.py`에는 3단계 폴백이 들어 있습니다.

```
① 일반 접속 (네이버)  →  ② 야후 파이낸스  →  ③ 우회 엔진 (insane-search)
```

**③번은 설치돼 있을 때만 작동합니다.** 기본 패키지에는 포함돼 있지 않습니다.

| | |
|---|---|
| 뚫는 것 | WAF·봇 탐지·외국 IP 차단 |
| **못 뚫는 것** | **국가 단위 네트워크 차단 (중국 GFW)** |

**VPN을 한국으로 켜면 한국에서 접속하는 것과 같아서 애초에 막히지 않습니다.**
그래서 실무에서는 ③번까지 갈 일이 거의 없습니다.

`--doctor`를 돌리면 설치 여부가 표시됩니다.

```
[없음  ] 우회 엔진 (insane-search)  — VPN 한국이면 필요 없습니다
```

정말 필요해지면(VPN을 켰는데도 네이버만 계속 막힐 때) 그때 설치하세요.
`pip install curl_cffi beautifulsoup4 pyyaml`가 추가로 필요합니다.

---

## B. 매일 하는 일 — 3단계 15분

### 1단계 · 데이터 뽑기 (1분)

종목 이름만 입력하면 됩니다.

```bash
python _tools/collect.py 삼성전자 > brief.md
```

한글 이름이 안 되면 6자리 숫자 코드로:

```bash
python _tools/collect.py 005930 > brief.md
```

이러면 `brief.md` 파일이 생깁니다. **여기에 필요한 데이터가 전부 들어 있습니다.**
- 현재가, 등락률, 거래량
- 코스피/코스닥 등락률, 상승·하락 종목 수
- 외국인·기관·개인 수급
- 차트 고점·저점·매물대·RSI
- **오늘 어떤 대본을 써야 하는지, 제목에 뭘 붙여야 하는지까지 자동 판정**

### 2단계 · 대본 만들기 (10분)

Codex를 이 폴더에서 열고, 아래 한 줄을 그대로 입력하세요.

```
brief.md 읽고 대본 써줘. AGENTS.md 규칙 전부 지켜서.
대본 + 제목 10개 + 썸네일 문구 + 설명란 + 태그까지 한 번에.
```

그러면 나옵니다:
- 대본 4,000자 (약 9분 30초)
- 제목 후보 10개
- 썸네일에 쓸 글자
- 설명란 (복붙용)
- 태그 12개

**주식 용어를 몰라도 됩니다.** AGENTS.md에 다 적혀 있어서 Codex가 알아서 씁니다.

### 3단계 · 검증 (4분) ★이건 절대 건너뛰면 안 됩니다

대본을 `대본.txt`로 저장한 뒤:

```bash
python _tools/validate.py 대본.txt brief.md
```

**둘 중 하나가 나옵니다.**

```
   ✅  합격 — 업로드해도 됩니다
```
→ `합격.txt` 파일이 생깁니다. **이 파일이 있어야 업로드할 수 있습니다.**

```
   ❌  불합격 — 3건.  고쳐서 다시 돌리세요

 1. [브리프에 없는 숫자]  「812만」  …외국인이 812만 주를 던졌고…
    → brief.md에 없는 숫자입니다. AI가 지어냈을 수 있으니 삭제하거나 브리프 값으로 고치세요
```
→ **화면에 나온 내용을 그대로 복사해서** Codex에 붙여넣으세요.

```
검증에서 이렇게 나왔어. 전부 고치고 다시 검증해줘.
(붙여넣기)
```

`✅ 합격`이 나올 때까지 반복합니다. **보통 1~3번 만에 통과합니다.**

> ### ⚠️ 왜 이걸 꼭 해야 하나요
>
> AI가 **없는 숫자를 지어내는 일이 있습니다.** 주가나 수급 숫자를 그럴듯하게 만들어냅니다.
> 주식을 모르면 이걸 절대 못 알아봅니다. 그대로 올리면 시청자가 확인하고 바로 지적합니다.
>
> 검증기는 대본의 **모든 숫자를 brief.md와 대조**합니다. 없는 숫자는 무조건 걸립니다.
> **이게 이 도구의 가장 중요한 기능입니다.**

### 4단계 · TTS (1분) ★파일을 잘못 넣으면 영상 다시 찍어야 합니다

합격하면 **`TTS용_대본.txt`** 파일이 자동으로 생깁니다.

> ## 일레븐랩스에는 `TTS용_대본.txt` 만 넣으세요.

**절대 이렇게 하지 마세요.**

| ❌ 하면 안 되는 것 | 무슨 일이 벌어지나 |
|---|---|
| Codex 화면에서 대본을 그대로 복사 | `[00:00]` `**굵게**` 를 **소리 내어 읽습니다** |
| `.md` 문서에서 복사 | 별표·대괄호·타임코드 전부 읽습니다 |
| 대본.txt(원본)를 그대로 사용 | 숫자 콤마를 쉼표로 끊어 읽습니다 |

`TTS용_대본.txt`는 검증기가 아래를 **자동으로 정리해서** 만든 파일입니다.

- 타임코드 `[00:00]` 제거
- 마크다운 `**` `[]` `#` `>` 제거
- 숫자 콤마 제거 (`4,036만` → `4036만`)
- 영문을 한글로 (`HBM` → `에이치비엠`, `SK` → `에스케이`)
- `%` → `퍼센트`, `~` → `에서`, `·` → 쉼표
- 문장 끝 마침표 보강

음성 받으면 영웅문 차트 띄우고 녹화하시면 됩니다.

### 5단계 · 업로드

제목·설명란·태그를 유튜브에 붙여넣으면 끝입니다.

---

## C. 장중에 촬영할 때

**따로 할 게 없습니다.** 도구가 지금 몇 시인지 보고 알아서 맞춥니다.

| 지금 시각(한국) | 자동으로 정해지는 것 | 제목 태그 |
|---|---|---|
| 새벽~08:59 | 개장전 검증형 | `*새벽 속보*` |
| 09:00~15:19 | **장중형 (실시간 현재가)** | `*5분전 속보*` |
| 15:20~15:59 | 장마감형 | `*장마감 속보*` |
| 16:00~ | 시간외형 | `*시외 속보*` |
| 주말 | 개장전 검증형 | `*새벽 속보*` |

### ⚠️ 장중엔 이것만 조심하세요

`brief.md`에 🚨 표시가 뜰 때가 있습니다.

> 🚨 **이 수급은 오늘 것이 아니라 26.07.27 것입니다.**

이건 **시장 전체 외국인·기관 수급이 아직 오늘 걸로 안 나왔다**는 뜻입니다. 장이 끝나야 확정되거든요.
이 표시가 뜨면 Codex에게 이렇게 말하세요.

```
시장 전체 수급은 오늘 게 아니니까 그 문단은 빼고 써줘.
종목별 수급만 써.
```

**절대 어제 숫자를 오늘 것처럼 쓰면 안 됩니다.** 시청자가 확인하면 바로 들통납니다.

### 제목의 "N분전"은 바꿔 쓰세요

`*5분전 속보*`가 기본이지만 **매번 같은 숫자를 쓰면 티가 납니다.**
`3분전` `5분전` `6분전` `7분전`을 돌려가며 쓰세요. 그리고 **실제로 그 시간대에 업로드**해야 합니다.

---

## D. 문제가 생기면

| 증상 | 원인 | 해결 |
|---|---|---|
| `접속 실패` | 한국 서버 차단 | VPN 켜고 `--doctor` 다시 |
| `종목코드를 못 찾았습니다` | 이름 오타 | 6자리 숫자 코드로 입력 |
| `수집 실패`가 일부 항목에 | 일시적 오류 | 1분 뒤 다시 실행 |
| `python: command not found` | Python 미설치 | A-1단계 다시 |
| 한글이 깨짐 | 터미널 인코딩 | 아래 명령 먼저 실행 |

한글 깨질 때 (Windows):
```bash
chcp 65001
```

---

## E. 절대 하면 안 되는 것

1. **숫자를 직접 고치지 마세요.** brief.md에 있는 것만 씁니다. 없으면 그 문장을 뺍니다.
2. **"얼마까지 간다"고 쓰지 마세요.** 목표가·수익률 제시는 금지입니다.
3. **"사세요/파세요"라고 하지 마세요.** "확인해야 할 조건"만 말합니다.
4. **뉴스를 지어내지 마세요.** brief.md에 없는 사건은 언급하지 않습니다.
5. **AGENTS.md / CLAUDE.md 를 지우거나 옮기지 마세요.** 이게 없으면 규칙이 안 걸립니다.
6. **같은 날 여러 편 올릴 때 제목 세 번째 칸(오늘의 재료)을 반드시 다르게 쓰세요.** 안 그러면 우리 영상끼리 검색에서 부딪힙니다.

---

## F. 관리자용 — 배포 전 확인

### 배포 패키지에 들어가야 하는 것

```
안교수카피/
├── AGENTS.md                  ★ Codex용 — 필수
├── CLAUDE.md                  ★ Claude Code용 — 필수
├── 06_직원용_운영매뉴얼.md      ← 이 문서
├── _tools/
│   ├── collect.py             ★ 필수
│   └── gueoche-guard/check.py ★ 필수
└── 00~05 문서                  (참고용, 없어도 동작)
```

> **`AGENTS.md`가 핵심입니다.** Codex는 `CLAUDE.md`를 읽지 않습니다.
> 직원들이 Codex를 쓴다면 `AGENTS.md`가 없으면 규칙이 하나도 안 걸립니다.
> 두 파일은 내용이 같으니 둘 다 넣어 두면 어느 도구를 쓰든 됩니다.

### 규칙을 어떻게 강제하나 — 5단 방어

| 단계 | 무엇을 하나 | 어길 수 있나 |
|---|---|---|
| **1. CLAUDE.md / AGENTS.md** | 세션마다 **자동 로드**되는 규칙 지시서 | ⚠️ AI가 어겨도 아무 일 없음 |
| **2. 자동 검증 훅** | 대본 `.txt` 저장 즉시 **validate.py 자동 실행** | ❌ **못 어김** (Claude Code 한정) |
| **3. validate.py** | 통과 못 하면 종료코드 1, `합격.txt`·`TTS용_대본.txt` 안 생김 | ❌ **못 어김** |
| **4. 합격.txt 규칙** | 이 파일 없으면 업로드 금지 (사내 규칙) | 직원이 무시 가능 → 5번으로 감시 |
| **5. 검증로그.tsv** | 누가 언제 몇 건 불합격했는지 자동 기록 | ❌ **못 지움** (덧쓰기 전용) |

**1번과 2번은 다른 물건입니다.**
`CLAUDE.md`는 "AI야 이렇게 써줘"라는 **지시**이고, 2번 훅은 "제대로 썼는지 **검사**"입니다.
지시만 있으면 AI가 어겨도 그냥 넘어갑니다. 그래서 둘 다 있어야 합니다.

### 2번 자동 검증 훅 — 설정 완료

`.claude/settings.json`에 걸어 뒀습니다. **대본 `.txt` 파일을 저장하는 순간 자동으로 검증이 돌아갑니다.**

```
합격 →  ✅ 대본 검증 통과 — 대본.txt
불합격 → ❌ 대본 검증 불합격 (Claude가 자동으로 고칩니다)
        + 불합격 내역이 AI에게 되돌아가 스스로 수정
```

검사 대상은 **1,500자 넘는 `.txt` 파일**만입니다. 메모나 짧은 파일은 건드리지 않습니다.

> ⚠️ **이 훅은 Claude Code 기능입니다.** 직원들이 쓰는 **Codex에서도 같은 방식으로 동작하는지는 확인이 필요합니다.**
> Codex에서는 `AGENTS.md`가 AI에게 **직접 `validate.py`를 실행하라고 지시**하는 방식으로 걸립니다.
> 배포 전에 직원 한 명에게 테스트해 보고, 훅이 안 걸리면 3단계(수동 실행)를 반드시 지키게 하세요.

**관리자가 볼 것: `검증로그.tsv`**

```
시각                사용자      파일          결과    불합격건수  분량   항목
2026-07-28 21:10   employee1  대본.txt      불합격   3        3820  브리프에 없는 숫자;예고 필러
2026-07-28 21:17   employee1  대본.txt      합격     0        3940
```

엑셀로 열립니다. 여기서 볼 것:
- **불합격 한 번도 없이 바로 합격** → 검증을 안 돌리고 로그만 맞춘 게 아닌지 확인
- **「브리프에 없는 숫자」가 자주 뜨는 직원** → AI에게 데이터를 제대로 안 주고 있음
- **같은 항목이 반복** → AGENTS.md를 그 항목 중심으로 보강

### 배포 전 점검

- [ ] 중국에 있는 직원 1명에게 먼저 `--doctor` 돌려보게 하고 결과 받기
- [ ] VPN이 필요하면 회사 차원에서 지급 (개인 VPN은 불안정합니다)
- [ ] `AGENTS.md`의 「댓글 키워드」·「채널 이름」을 실제 채널에 맞게 수정
- [ ] 태그 ④갈래(채널 이름)에 실제 채널명 넣기
- [ ] 첫 3편은 관리자가 직접 검수하고 내보내기

### 데이터가 아닌 것 — 관리자가 알고 계셔야 합니다

도구가 뽑아주는 것 중 **3가지는 사실이 아니라 추정·해석**입니다.

| 항목 | 성격 |
|---|---|
| 주가·거래량·지수·수급 | **공개 데이터** — 검증 가능 |
| 고점·저점·이동평균·RSI | **계산값** — 정확 |
| **매물대** | **추정치** — 거래 밀집도로 근사한 값 |
| **"세력의 의도"** | **해석** — 데이터 아님 |
| **뉴스·거시 배경** | brief에 없음 — 넣으려면 별도 확인 필요 |

아래 두 줄은 매 편 대본에 들어갑니다. 빼지 마세요.

> `제가 뭐 무조건 얼마까지 간다 이런 약속은 드릴 수 없습니다.`
> 설명란 면책: `본 영상은 공개된 차트와 시장 자료를 바탕으로 제작된 정보 제공 콘텐츠입니다. 투자에 대한 최종 판단과 책임은 투자자 본인에게 있습니다.`

### 한 가지 더

원본 채널은 TTS 합성음을 씁니다. 하루 9편 양산의 비결이지만,
**유튜브는 투자 조언을 하는 AI 진행자에 대해 수익 창출을 제한합니다**(hermes 매뉴얼 슬라이드 17).

대응은 매뉴얼에 적힌 그대로입니다 — *실제 대표가 자기 관점을 자기 이름으로 말하는 구조라는 점을 목소리와 얼굴, 표기로 남겨둔다.*
양산 속도를 택할지 수익화를 택할지는 **배포 전에** 정하시는 게 좋습니다. 나중에 바꾸면 이미 올린 영상 전체가 영향을 받습니다.
